# Copy values into Streamlit Cloud → App settings → Secrets
# (do not commit real secrets.toml with private hosts if sensitive)

# Public FastAPI host that serves the React SPA at /ui/
# After deploying the root Dockerfile to Render/Railway/Fly:
EDGR_PUBLIC_UI_URL = "https://YOUR-SERVICE.onrender.com/ui/"
